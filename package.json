{
  "name": "guitar-api",
  "version": "1.0.0",
  "description": "RESTful API for guitars that uses Express and SuperTest",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "mocha ./tests/*.spec.js",
    "test:watch": "nodemon --watch tests/ --exec 'npm test'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22"
  }
}